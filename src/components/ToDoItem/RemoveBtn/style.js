import styled from 'styled-components';

const RemoveBtn = styled.button`
  width: 40px;
  background-color: transparent;
  justify-self: end;

  &:hover {
    cursor: pointer;
  }
`;

export { RemoveBtn };
